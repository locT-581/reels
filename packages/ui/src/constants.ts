/**
 * UI Component Constants
 */

/**
 * Icon sizes for UI components
 * Uses UPPERCASE to match existing code convention
 */
export const ICON_SIZE = {
  SM: 16,
  MD: 24,
  LG: 32,
  XL: 48,
  // Lowercase aliases for convenience
  sm: 16,
  md: 24,
  lg: 32,
  xl: 48,
} as const

